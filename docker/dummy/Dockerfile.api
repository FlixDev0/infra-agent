FROM python:3.11-slim
RUN pip install fastapi uvicorn
RUN echo "from fastapi import FastAPI; app = FastAPI()\n@app.get('/health')\ndef h(): return {'status':'ok'}" > main.py
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "80"]
